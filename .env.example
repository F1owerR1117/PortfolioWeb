# 管理员注册秘钥
ADMIN_SECRET=AdminKey123

# Session 密钥（⚠️ 必须修改为随机字符串，否则服务拒绝启动）
SESSION_SECRET=change-this-to-a-random-secret

# 服务端口
PORT=3000

# 数据库文件路径（相对于项目根目录）
DB_PATH=./database.db

# 允许创建新数据库（首次运行时设置）
ALLOW_NEW_DB=1
